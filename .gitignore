
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

node_modules/
dist/
*.sqlite
*.sqlite-*

# Local agent tooling — hookify local rules + sync manifest (never publish)
.claude/*.local.md
.claude/.hookify-sync-manifest.json

# IDE / npm (this project pins deps with bun.lock; npm install is also supported)
.idea/
package-lock.json
