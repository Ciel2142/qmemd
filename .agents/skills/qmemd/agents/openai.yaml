interface:
  display_name: "Qmemd"
  short_description: "Repo-specific patterns and workflows for qmemd"
  default_prompt: "Use the qmemd repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true